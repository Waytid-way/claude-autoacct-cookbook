# App Configuration
APP_MODE=DEV  # DEV = Mock data, PROD = Real Claude API
LOG_LEVEL=info  # debug | info | warn | error

# Claude API (required for PROD mode only)
CLAUDE_API_KEY=sk-ant-api03-xxxxxxxxxxxxx

# Notes:
# - DEV mode is free and instant (uses mock data)
# - PROD mode requires a valid Claude API key from https://console.anthropic.com/
# - Cost: ~฿0.50 per receipt with Claude 3.5 Sonnet
