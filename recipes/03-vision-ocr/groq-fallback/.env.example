# App Mode
APP_MODE=DEV  # DEV = Mock, PROD = Real APIs

# Claude API (for complex receipts)
CLAUDE_API_KEY=sk-ant-api03-xxxxx

# Groq API (for simple receipts)
GROQ_API_KEY=gsk-xxxxx

# Strategy Config
SIMPLE_CONFIDENCE_THRESHOLD=0.85  # Min confidence to use Groq
ENABLE_FALLBACK=true              # Groq fail → Claude

# Notes:
# - DEV mode uses mock adapters (free, instant)
# - PROD mode requires both API keys
# - Groq is ~10x cheaper than Claude (฿0.05 vs ฿0.50)
# - Hybrid saves ~70% cost on average
